<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.server.mappers.TestMapper">
<insert id="add"  useGeneratedKeys="true" keyProperty="studentid" parameterType="Student">
	insert into t_student(name,age) values(#{name},#{age});
<!-- 	<selectKey resultType="int" keyProperty="id" order="AFTER">
			SELECT
			LAST_INSERT_ID() as id
		</selectKey> -->
</insert>
<!-- <insert id="insertMarketList" useGeneratedKeys="true"
		keyProperty="id" parameterType="java.util.List">
		insert into
		marketList(father,son,type,notes,temp1,temp2,syncer,syncing,syncTime)
		values
		<foreach collection="list" item="item" index="index"
			separator=",">
			(#{item.father},#{item.son},#{item.type},#{item.notes},#{item.temp1},#{item.temp2},#{item.syncer},#{item.syncing},#{item.syncTime})
		</foreach>
		<selectKey resultType="int" keyProperty="id" order="AFTER">
			SELECT
			LAST_INSERT_ID() as id
		</selectKey>
	</insert> -->
</mapper>