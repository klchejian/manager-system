<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.server.mappers.GradeMapper">

<select id="getMyCourseGrade" parameterType="map" resultType="map">
	SELECT *FROM t_courseselect where studentNum = #{studentNum}
</select>

</mapper>